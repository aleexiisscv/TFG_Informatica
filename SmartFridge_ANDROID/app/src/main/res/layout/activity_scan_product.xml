<?xml version="1.0" encoding="utf-8"?>
<!--
    Vision Artificial (interfaz preparada, modelo pendiente).

    La pantalla ya resuelve TODO lo que no depende del modelo: permiso
    de camara, invocacion de la camara del sistema, previsualizacion de
    la captura y confirmacion. Cuando exista el clasificador, el unico
    cambio sera sustituir el cuerpo de onImagenCapturada() por la
    inferencia (en dispositivo con TFLite/ML Kit, o subiendo el bitmap
    al backend). La camara del movil actua como plan de contingencia de
    la camara Edge AI del propio frigorifico.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:fitsSystemWindows="true"
        app:elevation="0dp">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:navigationContentDescription="@string/common_back"
            app:navigationIcon="@drawable/ic_arrow_back_24"
            app:title="@string/scan_title" />
    </com.google.android.material.appbar.AppBarLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="@dimen/space_l"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <com.google.android.material.card.MaterialCardView
            style="@style/Widget.SmartFridge.Card"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">

                <ImageView
                    android:id="@+id/scanPlaceholder"
                    android:layout_width="120dp"
                    android:layout_height="120dp"
                    android:layout_gravity="center"
                    android:contentDescription="@null"
                    android:src="@drawable/ic_scan_frame"
                    app:tint="?attr/colorOutline" />

                <ImageView
                    android:id="@+id/scanPreview"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:contentDescription="@string/scan_preview_desc"
                    android:scaleType="centerCrop"
                    android:visibility="gone" />
            </FrameLayout>
        </com.google.android.material.card.MaterialCardView>

        <TextView
            android:id="@+id/scanExplanation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_m"
            android:text="@string/scan_explanation"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="?attr/colorOnSurfaceVariant" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/captureButton"
            style="@style/Widget.SmartFridge.Button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_m"
            android:text="@string/scan_open_camera"
            app:icon="@drawable/ic_camera_24" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/confirmButton"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_s"
            android:minHeight="56dp"
            android:text="@string/scan_confirm"
            android:visibility="gone"
            app:icon="@drawable/ic_add_24" />
    </LinearLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
