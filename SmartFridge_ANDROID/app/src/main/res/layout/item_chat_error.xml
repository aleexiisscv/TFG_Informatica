<?xml version="1.0" encoding="utf-8"?>
<!--
    Aviso de error dentro de la conversación, con reintento.

    Se muestra en la lista en lugar de como Snackbar por una razón de
    uso: un Snackbar desaparece a los pocos segundos, y si el usuario
    estaba mirando otra cosa cuando falló la petición, vuelve y no
    entiende por qué no hay respuesta. Aquí el fallo queda registrado en
    el hilo, en su sitio cronológico, y el botón de reintentar sigue
    disponible.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="@dimen/space_xs"
    android:paddingBottom="@dimen/space_xs">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:layout_marginEnd="40dp"
        android:background="@drawable/bg_bubble_in"
        android:backgroundTint="?attr/colorErrorContainer"
        android:orientation="vertical"
        android:paddingStart="@dimen/space_m"
        android:paddingTop="12dp"
        android:paddingEnd="@dimen/space_m"
        android:paddingBottom="@dimen/space_s">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:contentDescription="@null"
                android:src="@drawable/ic_warning_24"
                app:tint="?attr/colorOnErrorContainer" />

            <TextView
                android:id="@+id/errorText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/space_s"
                android:textAppearance="?attr/textAppearanceBodyMedium"
                android:textColor="?attr/colorOnErrorContainer"
                tools:text="No se pudo conectar con el servidor" />
        </LinearLayout>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/retryChatButton"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_xs"
            android:text="@string/dashboard_retry"
            android:textColor="?attr/colorOnErrorContainer"
            app:icon="@drawable/ic_refresh_24"
            app:iconTint="?attr/colorOnErrorContainer" />
    </LinearLayout>
</FrameLayout>
